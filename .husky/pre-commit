# Lint + format staged files (eslint --max-warnings=0 + prettier)
npx lint-staged

# Type check the whole project — staged-file linting cannot catch type errors
# that a change introduces elsewhere.
npx tsc --noEmit
