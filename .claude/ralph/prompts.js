// Every word the agent is ever told, in one place — four passes, one shared
// rule set:
//   buildPrompt()          implement the issue            (full Edit/Write)
//   buildFixPrompt()       fix one review finding         (full Edit/Write)
//   buildReviewPrompt()    post-DONE self-review          (read-only)
//   buildCodeReviewPrompt() code-review skill pass        (read-only + Skill)
//
// Nothing here executes anything: these are string builders. The permissions
// each pass actually gets are written separately by workspace.js, and the
// sentinel lines these prompts demand are parsed by parsing.js — change a
// sentinel here and you must change it there too.
//
// Module map for the whole loop: see the header of core.js.

// Shared by buildPrompt() (fresh implementation) and buildFixPrompt() (a
// point fix requested by the post-DONE review below) — both need the exact
// same standing rules (organizational-protocol exclusions, BLOCKED
// conditions, mutation-testing/real-file discipline, final-answer format),
// only the opening framing and DONE-triggering task differ.
function buildTaskRules(maxTurns, skillNames = []) {
  return [
    'Реализуй задачу строго согласно Context, Affects, Docs to Read, Key Invariants, Acceptance Criteria, Test Requirement и Definition of Done из тела issue выше и из корневого CLAUDE.md. После финальных изменений обязательно прогоняй `npm run check` (это lint:strict + format:check + tsc --noEmit) — он должен быть зелёным. Если изменения могут повлиять на сборку (новые импорты, server/client-компоненты, next.config, env-переменные) — прогоняй ещё и `npm run build`: именно он ловит то, что реально валит прод-деплой на Vercel.',
    '',
    skillNames.length > 0
      ? `В этом репозитории установлены скиллы (\`.claude/skills/\`), доступные тебе через Skill tool: ${skillNames.join(', ')}. Прежде чем писать код по теме, которую покрывает один из них (например: задача про тесты → скилл \`vitest\`, задача про MUI/стили → \`material-ui-*\`), вызови этот скилл — он даёт проверенные паттерны/API вместо угадывания по памяти. Если ни один явно не подходит к этой конкретной issue — не вызывай скиллы просто "на всякий случай", это трата ходов.`
      : null,
    '',
    maxTurns != null
      ? `У тебя есть примерно ${maxTurns} ходов на всю задачу (сообщение + вызов инструмента = один ход), и на этом бюджет заканчивается — если ты не успеешь ответить DONE/BLOCKED до его исчерпания, вся проделанная работа теряется (следующий прогон начнётся с нуля, без памяти о том, что ты уже сделал). Не трать больше примерно трети бюджета на изучение кодовой базы перед тем, как начать писать реализацию — читай целенаправленно то, что реально нужно для этой задачи (issue уже указывает Docs to Read), а не исследуй широко "на всякий случай".`
      : null,
    '',
    '`npm install` уже выполнен контроллером в этой рабочей директории — не запускай его сам (и не нужно, `Bash(npm install)` не в списке разрешённых команд).',
    '',
    'Read-only git-команды разрешены и предназначены для самопроверки: `git status`/`git diff`/`git log` (но не `add`/`commit`/`push` — их нет и не будет). Перед тем как писать DONE, посмотри `git diff` на свои изменения так, как это сделал бы ревьюер со стороны — это дешёвый способ заметить случайно оставленный debug-код, забытый TODO или файл, изменённый по ошибке.',
    '',
    'ВАЖНО про CLAUDE.md: он написан для интерактивной работы Claude Code с человеком за клавиатурой. Ты работаешь автономно, внутри внешнего контроллера, и часть его правил к тебе НЕ применяется — не потому что их можно нарушать, а потому что их выполняет контроллер до и после тебя. Не пытайся их выполнять и не трать на них ходы:',
    '- **Plan-first** (составить план и ждать подтверждения "go") — не применяется: подтверждать некому, план уже утверждён в виде самого issue. Не останавливайся в ожидании ответа.',
    '- **Issue-first** (проверить/создать GitHub Issue) — уже сделано, issue существует, его тело приведено выше.',
    '- **Branch-first** (`git status`/`git branch`, переключение на main, `git pull`, создание ветки) — уже сделано контроллером: ты уже на правильной ветке от правильного base branch.',
    '- **Закрытие задачи** (отметить Acceptance Criteria, комментарий с test evidence на issue, "Closes #N" в описании PR, вопросы пользователю) — всё это делает контроллер/человек после тебя, на основе самоотчёта из формата ACCEPTANCE CRITERIA SELF-REPORT ниже. Не пытайся вызывать `gh issue comment`/`gh issue edit` (доступа к `gh` у тебя нет вообще) и не пытайся задавать вопросы пользователю — ответить некому.',
    '- **Git/PR order** (`git add`/`git commit`/`git push`/`gh pr create`) — делает контроллер, см. правило про отсутствие доступа к git/gh ниже.',
    '- **"Read First" → `gh issue view <n>`** — не нужно и не сработает: полное тело issue уже приведено выше между маркерами === ISSUE === / === END ISSUE ===.',
    '',
    'У тебя НЕТ живого окружения: нет поднятого `next dev`, нет доступа к реальной Supabase (ни к БД, ни к auth), нет браузера и нет инструментов Playwright. Поднимать dev-сервер в фоне запрещено — именно так один из прошлых прогонов оставил осиротевший процесс, державший файловые локи и уронивший контроллер. Поэтому любой пункт `## Test Requirement` или правило CLAUDE.md, которые требуют реально поднятого стека ("проверь в браузере", "прогони через настоящую БД", "проверь живой HTTP-запрос", "сверь визуально") — ты выполнить не можешь. НЕ блокируйся из-за этого и НЕ имитируй живую проверку кодом, который её не покрывает. Сделай всё, что реально в твоих силах (`npm run check`, `npm run build`, чтение кода), а в самоотчёте перед DONE явно перечисли, какой конкретно пункт остаётся непокрытым и требует ручной проверки человеком на Vercel preview уже после создания PR — не отмечай его как выполненный.',
    '',
    'При этом ВСЁ содержательное из CLAUDE.md применяется к тебе в полном объёме, и его нарушение — это плохо сделанная задача: структура проекта и границы слоёв (`src/app` / `src/components` / `src/hooks` / `src/services` / `src/strategies` / `src/lib`), правила работы с Supabase и с демо-режимом, правило про обратную совместимость схемы БД, конвенции TypeScript/MUI и общий стиль кода. Кратко: организационные протоколы вокруг задачи — не твои; правила о том, каким должен быть сам код, — твои.',
    '',
    'Отдельно про прод: `main` этого репозитория автоматически деплоится на Vercel, а Supabase — один и тот же для прода и для preview. Поэтому изменение, которое ломает сборку или меняет форму данных так, что старый уже задеплоенный код перестаёт работать, — это не "поправим после мержа", а падение живого приложения. Предпочитай аддитивные изменения (новое поле/новый компонент/новая ветка логики) ломающим (переименование/удаление/сужение типа существующего). Если задача по существу требует ломающего изменения — это допустимо, но явно скажи об этом в SUMMARY.',
    '',
    'Если `npm run check` или `npm run build` остаются красными после 5 попыток исправить — не продолжай бесконечно, заверши ответ строкой `BLOCKED: <описание проблемы>`.',
    '',
    'Если для продолжения нужно решение человека — формулировка issue неоднозначна, есть несколько разумных вариантов реализации и непонятно, какой правильный, или чего-то не хватает в Context/Acceptance Criteria — НЕ гадай и НЕ выбирай вариант сам. Заверши ответ строкой `BLOCKED: <какое решение нужно и почему неоднозначно>`. Ты работаешь без присмотра — нет человека, который прямо сейчас подтвердит твоё предположение, поэтому неверное предположение хуже, чем остановка.',
    '',
    'Если в процессе реализации выясняется, что буквальное выполнение Acceptance Criteria и одновременное соблюдение всех Key Invariants из этой же issue физически невозможны без нарушения одного из них (например: способ, явно описанный в issue как решённый, на самом деле требует задеть данные/файлы/поведение, которые тот же issue запрещает трогать) — это НЕ повод тихо выбрать один пункт issue в ущерб другому и продолжить как ни в чём не бывало. Сформулируй противоречие явно и заверши ответ строкой `BLOCKED: <в чём конкретно конфликт между каким пунктом AC и каким Key Invariant>`. Молчаливый компромисс между двумя требованиями одной и той же issue — это ровно то же самое гадание за человека, которое запрещено правилом выше.',
    '',
    'Если для выполнения задачи требуется изменить схему базы данных или SQL-миграции (`docs/**/*.sql`, новые таблицы/колонки/политики RLS в Supabase) — НЕ вноси эти изменения. Миграции применяются вручную человеком в Supabase SQL editor, против того же проекта, на котором работает живое приложение, поэтому ломающая миграция роняет прод мгновенно, ещё до мержа PR. Вместо этого заверши ответ строкой `BLOCKED-DB-CHANGE: <что именно и почему нужно поменять в схеме>`.',
    '',
    'Перед тем как завершить ответ строкой DONE — если ты добавлял тесты, среди своих новых тестов найди assertions именно высокого риска: проверка подстроки/слова (`toContain`/`toMatch`/`includes`) внутри БОЛЬШОГО свободного текста — целого файла, целого документа, многострочного контента. Это высокий риск, потому что короткое слово может случайно встретиться где-то ещё в тексте по несвязанной причине, и тогда тест зелёный независимо от того, работает ли проверяемая логика. Если таких assertions пять или меньше — проверь на мутации каждый. Если больше пяти — проверь первые пять (по одному на каждый отдельный блок, чтобы покрыть разные части логики) и не трать ходы на остальные. Мутационная проверка: временно вырежи/измени именно то, что assertion должен ловить, прогони тест, убедись что он ПАДАЕТ, верни файл обратно. Это НЕ относится к проверкам короткого сообщения или label, которые твой же код только что сформировал специально под этот тест-кейс — там нет риска случайного совпадения.',
    '',
    'Если твой код читает/парсит/анализирует реальные файлы или реальные данные, уже существующие в этой рабочей директории (например, файлы из `src/data`, а не только придуманные тобой строки) — прогони свой код на этих реальных данных и посмотри на результат, прежде чем считать логику готовой. Проверка на искусственных 2-3-строчных примерах, которые ты сам написал, не доказывает, что логика работает на настоящих данных — она лишь доказывает, что она делает то, что ты задумал в изолированном примере. Если результат на реальных данных выглядит бессмысленно — это баг в логике, который нужно исправить до DONE, а не то, что можно узнать только после мержа.',
    '',
    'Если Test Requirement, Acceptance Criteria или Docs to Read в issue ссылаются на файл/путь/директорию, которых на самом деле не существует в этой рабочей директории — это классический случай неоднозначности из правила выше (issue написан с ошибочным предположением). НЕ подставляй тихо синтетическую замену и не придумывай собственную интерпретацию того, что автор issue «на самом деле имел в виду» — заверши ответ строкой `BLOCKED: <какой путь не существует и что нужно уточнить>`.',
    '',
    'Если для реализации нужно прочитать данные/логику, уже определённые в другом файле (например, константы, конфиг, тип), и прямой импорт натыкается на препятствие (например, у файла есть побочный эффект при импорте) — сначала попробуй устранить само препятствие в исходном файле, а не копировать данные в новый файл с комментарием «синхронизируй вручную». Ручное дублирование источника правды — это тихий дрейф между копиями, то есть ровно тот класс бага, который ты, возможно, и должен предотвратить. Дублирование допустимо только если реальное препятствие невозможно убрать в рамках текущей задачи — и тогда явно объясни в SUMMARY, почему.',
    '',
    'У тебя НЕТ доступа к git commit/push/worktree и НЕТ доступа к gh — не пытайся их использовать, коммит/push/PR делает внешний контроллер после тебя. Твоя единственная задача — отредактировать файлы и прогнать проверки в этой директории.',
    '',
    'НЕ редактируй `.claude/settings.json` и `.claude/settings.local.json`, чтобы выдать себе дополнительные права (например, доступ к git/gh) — это осознанное ограничение, а не случайный пробел, и попытка обойти его — грубое нарушение, а не решение проблемы. Если тебе не хватает какого-то конкретного разрешения — это `BLOCKED` (см. правило про отказ в правах ниже), не повод редактировать файлы настроек.',
    '',
    'Если какой-то инструмент или команда отклонена из-за прав доступа (permission denied / "not permitted" / "not in the allowed list") — это ограничение Claude Code, а не проблема пути/shell/директории. Повторная попытка той же команды другим синтаксисом (другой shell, `cd`/`Set-Location`, `--prefix`, heredoc и т.п.) НЕ поможет — это тот же самый отказ. Не трать на это больше одной попытки. Из этого правила два исключения. Первое — если отказ пришёл именно на попытку отредактировать файл в `docs/`: это не баг permissions, это тот же самый случай "нужно менять схему БД/миграции" из правила выше, просто он проявился как отказ прав. В этом случае заверши строкой `BLOCKED-DB-CHANGE: <что именно и почему>`, а не `BLOCKED`. Второе — если отказ пришёл на попытку удалить/переименовать/переместить файл (`rm`, `Remove-Item`, `mv`, `git mv`, `node -e "...unlinkSync..."` и т.п.) — см. следующее правило про маркер `DEL_RALPH`, это тоже не `BLOCKED`. Для любого другого отказа прав сразу заверши строкой `BLOCKED: доступ отклонён для <что именно> — нужно расширить permissions.allow`.',
    '',
    'Особый случай — нужно убрать или переименовать уже существующий файл (например, миграция файловой конвенции: `src/middleware.ts` → `src/proxy.ts`). У тебя нет и не будет прав на удаление/перемещение файлов — не пытайся обойти это через Bash в любом синтаксисе и не заканчивай ответ из-за этого строкой `BLOCKED`. Вместо этого: создай новый файл как обычно (`Write`), а в файле, который нужно убрать, первой строкой оставь маркер-комментарий `DEL_RALPH: <короткая причина>` (например `// DEL_RALPH: migrated to src/proxy.ts`), остальное содержимое файла не трогай. Контроллер сам физически переименует помеченный файл в `del_ralph_<имя>` сразу после твоего хода (это rename, не удаление — содержимое сохраняется и видно в git-истории) и сам перепроверит `npm run check`/`npm run build` уже в правильном состоянии. Из этого следует: если `npm run check`/`npm run build` красные ИМЕННО из-за того, что старый (помеченный) и новый файл одновременно существуют под конфликтующими именами — это ожидаемо для такой миграции, не пытайся это исправить в рамках своего хода и не считай блокером, просто заверши задачу как обычно с обычным самоотчётом. Если же сборка красная по другой, не связанной с этим файлом причине — действуй по обычному правилу (до 5 попыток, потом `BLOCKED`).',
    '',
    'Когда закончишь, ЗАВЕРШИ свой финальный ответ РОВНО одним из трёх вариантов, каждый на новой строке, БЕЗ markdown-разметки (никакого `**жирного**`, `` `кода` `` или других символов вокруг этих строк — контроллер ищет ровно эти литеральные строки):',
    '',
    '1) Если задача выполнена — НЕПОСРЕДСТВЕННО перед сентинель-строкой DONE выведи самоотчёт по Acceptance Criteria в СТРОГО следующем формате (контроллер его парсит и на основе него сам отмечает чек-боксы в issue и постит комментарий — формат должен совпадать буквально, никаких вариаций):',
    '=== ACCEPTANCE CRITERIA SELF-REPORT ===',
    '1. COVERED: <чем конкретно закрыт первый пункт — какой файл, тест, проверка>',
    '2. NOT VERIFIED: <почему второй пункт не может быть проверен тобой — например, требует живого браузера/Supabase>',
    '... (по одной строке на каждый пункт `## Acceptance Criteria` из issue, В ТОМ ЖЕ ПОРЯДКЕ, ровно столько строк, сколько там пунктов — не пропускай и не объединяй несколько пунктов в одну строку)',
    '=== END ACCEPTANCE CRITERIA SELF-REPORT ===',
    '',
    'Каждая строка — либо `N. COVERED: <конкретика>`, либо `N. NOT VERIFIED: <причина>`. `COVERED` пиши только если можешь указать конкретный файл/проверку, которые это подтверждают — не пиши `COVERED` для пункта, который на самом деле требует живого браузера/Supabase и который ты не можешь проверить в этой рабочей директории; для такого пункта пиши `NOT VERIFIED` честно, это не помешает написать DONE, но не даст контроллеру ложно отметить его как проверенный. Если общий ответ по пункту получается только словами "сделано"/"готово" без конкретики — это, скорее всего, `NOT VERIFIED`, а не `COVERED`. Дальше сама сентинель-строка:',
    'DONE',
    'TYPE: <feat|fix|docs|chore|refactor|test>',
    'SUMMARY: <короткое однострочное описание изменения, без номера issue>',
    '',
    '2) Если задача заблокирована (кроме случая с изменением схемы БД выше):',
    'BLOCKED: <причина>',
    '',
    '3) Если нужны изменения схемы БД / SQL-миграции (см. выше):',
    'BLOCKED-DB-CHANGE: <что и почему>',
  ];
}

function buildPrompt(chosen, maxTurns, skillNames = []) {
  return [
    `Ты реализуешь GitHub Issue #${chosen.id} в этой рабочей директории (уже на правильной ветке, ответвлённой от правильного base branch — не переключай и не создавай ветку).`,
    '',
    `=== ISSUE #${chosen.id}: ${chosen.title || ''} ===`,
    chosen.body || '(тело issue пустое)',
    '=== END ISSUE ===',
    '',
    ...buildTaskRules(maxTurns, skillNames),
  ].join('\n');
}

// Point-fix pass requested by the post-DONE review (see runIssue()) — same
// runDir/branch as the original DONE, same agent role and standing rules,
// but framed around fixing exactly what an independent reviewer (no shared
// memory, no Edit/Write access) found wrong, not redoing the whole task.
function buildFixPrompt(chosen, reviewFindings, maxTurns, skillNames = []) {
  return [
    `Ты дорабатываешь свою же предыдущую реализацию GitHub Issue #${chosen.id} в этой же рабочей директории (та же ветка, тот же клон, ничего не переключай и не создавай заново). Независимый ревьюер — отдельный запуск без доступа к Edit/Write и без общей с тобой памяти — проверил твой предыдущий ответ DONE и нашёл проблему, которую нужно исправить.`,
    '',
    `=== ISSUE #${chosen.id}: ${chosen.title || ''} ===`,
    chosen.body || '(тело issue пустое)',
    '=== END ISSUE ===',
    '',
    '=== ЧТО НАШЁЛ REVIEW (обязательно разобрать) ===',
    reviewFindings,
    '=== END REVIEW ===',
    '',
    'Исправь именно то, что описано выше, точечно — не переделывай всю реализацию заново и не трогай то, что ревью не упомянул. Если, разобравшись, ты считаешь находку ложным срабатыванием — не меняй код ради самого изменения; опиши в самоотчёте перед DONE, почему находка не применима, и оставь код как есть, объяснение попадёт в SUMMARY. Если находка реальна, но её нельзя исправить, не нарушив что-то другое из этой же issue — это BLOCKED (см. правило про конфликт AC/Key Invariants ниже), а не молчаливый компромисс.',
    '',
    ...buildTaskRules(maxTurns, skillNames),
  ].join('\n');
}

// Post-DONE self-review pass (see runIssue()) — a SEPARATE, fresh `claude -p`
// invocation with read-only permissions (writeReviewerPermissions()), so it
// has no way to "helpfully" patch its way to a false PASS. Exists because
// tsc/lint/test/test:e2e all being green does not prove the diff actually
// satisfies the issue's Key Invariants — found the hard way on ISSUE-287's
// own autonomous run, which passed all four and still silently deactivated
// real dev-DB rows the issue explicitly said not to touch. This prompt asks
// the same kind of question a human `/code-review` pass would, scoped
// specifically to the failure classes already observed across Ralph runs.
function buildReviewPrompt(chosen, diffText) {
  return [
    `Ты проверяешь уже готовую реализацию GitHub Issue #${chosen.id} — НЕ дописывай и не исправляй код, у тебя нет прав на Edit/Write, только на чтение и диагностические команды (git diff/log/show, npm run test/lint, npx tsc/*).`,
    '',
    `=== ISSUE #${chosen.id}: ${chosen.title || ''} ===`,
    chosen.body || '(тело issue пустое)',
    '=== END ISSUE ===',
    '',
    '=== DIFF (git diff HEAD) ===',
    diffText || '(диф пуст)',
    '=== END DIFF ===',
    '',
    'Другой агент (без общей с тобой памяти) только что реализовал эту issue и сам заявил, что всё сделано и все проверки (tsc/lint/test) зелёные. Зелёные проверки НЕ доказывают, что реализация соответствует issue — несколько раз уже случалось, что тесты проходили, а реализация тихо нарушала явно прописанный в issue инвариант или не ловила ту регрессию, ради которой писалась. Твоя задача — независимо перепроверить именно это, а не повторно гонять те же команды и доверять их результату на слово.',
    '',
    'Проверь диф по каждому из следующих пунктов и держи в голове, что "тесты зелёные" — не аргумент ни по одному из них:',
    '',
    '1. **Key Invariants из issue соблюдены буквально, не только по духу.** Если issue говорит "не трогать X" — найди в дифе любое место, где X читается, изменяется или удаляется, даже временно (например, "выключить и потом включить обратно" — это тоже "трогать"). Если invariant явно нарушен — это FAIL, независимо от того, что тесты проходят.',
    '2. **Acceptance Criteria выполнены по существу, а не только по факту наличия кода с похожим названием.** Если AC требует конкретную проверку (например, "убедиться, что тест падает при поломке X") — в дифе/логах должны быть настоящие следы того, что эта проверка правда выполнялась (не просто текстовый комментарий "проверено"), а не быть на честном слове самоотчёта другого агента.',
    '3. **Риск false-negative тестов**: assertions вида `toContain`/`toMatch`/`includes` по большому свободному тексту (файл целиком, многострочный prose) — временно испорти именно то, что assertion должен ловить, прогони тест, убедись что он ПАДАЕТ, верни обратно. Если у тебя нет прав что-то менять для этой проверки — опиши, что нашёл, и является ли это риском, вместо того чтобы промолчать.',
    '4. **Реальные данные, а не только придуманные фикстуры**, если код читает/парсит что-то из реального репозитория (например, файлы из `src/data`) — проверь, что логика прогонялась и осмысленно работает на настоящих файлах, а не только на 2-3-строчных синтетических примерах.',
    '5. **Дублирование источника правды**: если что-то скопировано вручную из другого файла вместо того, чтобы устранить реальное препятствие к прямому использованию оригинала — отметь это.',
    '',
    'Не придирайся к стилю/оформлению — ищи именно содержательные расхождения с issue и реальные риски ложноположительных тестов, не косметику.',
    '',
    'Заверши ответ РОВНО одной из двух строк, каждая на новой строке, БЕЗ markdown-разметки вокруг неё:',
    '',
    '1) Если содержательных проблем не найдено:',
    'REVIEW: PASS',
    '',
    '2) Если найдена хотя бы одна содержательная проблема (нарушение Key Invariant, AC не выполнен по существу, недоказанная критичная проверка, тест не ловит то, для чего писался):',
    'REVIEW: FAIL: <конкретно что и где — файл/строка, какой пункт issue нарушен>',
  ].join('\n');
}

// Second, independent post-DONE pass — runs AFTER self-review (buildReviewPrompt())
// passes, not instead of it. Self-review is deliberately narrow (Key Invariants,
// AC substance, false-negative test risk, real-data testing, source-of-truth
// duplication) and explicitly excludes style/simplification ("не придирайся к
// стилю/оформлению" in buildReviewPrompt()) — this pass exists to cover exactly
// that gap via the project's own `code-review` skill (correctness bugs +
// reuse/simplification/efficiency), the same tool a human would run before
// merging. Found live on ISSUE-317: self-review passed a diff that duplicated
// ~50 lines of security-sensitive path-safety logic between two sibling
// controller methods — outside self-review's own checklist by design, but
// exactly what `code-review` is for.
//
// Same read-only-agent pattern as the self-review pass: no Edit/Write, only
// permission to run the skill itself and read-only git/test/lint commands
// (writeCodeReviewPermissions()) — this pass reports findings, it never
// applies them; a real fix still goes through buildFixPrompt() with full
// Edit/Write access, then gets re-reviewed by this same pass before it's
// accepted (see the loop in runIssue()).
function buildCodeReviewPrompt(chosen) {
  return [
    `Ты проверяешь уже готовую и прошедшую self-review реализацию GitHub Issue #${chosen.id} — у тебя нет прав на Edit/Write, только на чтение, диагностические команды и вызов скилла code-review.`,
    '',
    `=== ISSUE #${chosen.id}: ${chosen.title || ''} ===`,
    chosen.body || '(тело issue пустое)',
    '=== END ISSUE ===',
    '',
    'Вызови скилл `code-review` (через Skill tool, имя скилла `code-review`, effort `medium`) без аргументов — он сам возьмёт текущий diff. Не передавай `--fix`/`--comment` — твоя задача только сообщить находки, не исправлять их самому и не постить их куда-либо.',
    '',
    'ВАЖНО: скилл может увидеть diff шире, чем изменения только этой issue — например весь стек коммитов текущей ветки против main, если несколько issue уже стекированы друг на друга (это нормально и ожидаемо, так виднее реальные проблемы). Он может найти что-то реальное в коде, который относится к ДРУГОЙ, уже закрытой и смёрженной issue этого же эпика — но чинить такую находку сейчас, в рамках текущей issue, ОПАСНО: тот код мог быть специально таким по архитектурному решению (Key Invariant) той, другой issue, которое ты сейчас не видишь и не должен переигрывать в одиночку без человека. Один раз это уже привело к тому, что фикс-агент слил специально разделённые модули в один класс и заодно сломал error-handling из другой задачи — не повторяй эту ошибку.',
    '',
    `Поэтому для КАЖДОЙ находки скилла определи, относится ли её файл к разделу "## Affects" ЭТОЙ issue (#${chosen.id}, см. тело issue выше) — сверься со списком файлов дословно, не по памяти:`,
    '- Находка на файле ИЗ "## Affects" этой issue — В СКОУПЕ.',
    '- Находка на файле НЕ из "## Affects" этой issue — ВНЕ СКОУПА (это работа для отдельной issue, см. правило корневого CLAUDE.md про находки вне скоупа активной issue).',
    '',
    'Заверши ответ РОВНО одной из следующих строк (каждая на новой строке, БЕЗ markdown-разметки вокруг неё):',
    '',
    '1) Находок вообще нет:',
    'CODEREVIEW: PASS',
    '',
    '2) Есть находки, но ВСЕ они вне скоупа этой issue (ни одна не на файле из её Affects) — не блокирует, просто зафиксируй:',
    'CODEREVIEW: PASS-OUT-OF-SCOPE: <находка 1: файл:строка — суть>; <находка 2: ...>; ...',
    '',
    '3) Есть хотя бы одна находка В СКОУПЕ этой issue (на файле из её Affects) — перечисли ТОЛЬКО находки в скоупе (находки вне скоупа сюда не включай, они не блокируют):',
    'CODEREVIEW: FAIL: <находка 1: файл:строка — суть>; <находка 2: ...>; ...',
  ].join('\n');
}

module.exports = {
  buildTaskRules,
  buildPrompt,
  buildFixPrompt,
  buildReviewPrompt,
  buildCodeReviewPrompt,
};
